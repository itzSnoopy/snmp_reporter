on:
  push:
    branches:
      - main
      
  workflow_dispatch:

jobs:
  publish:
    name: publish
    runs-on: [dev]

    steps:
      - name: Pull new service
        run: cd ${{ secrets.PROD_REPO_DIR }} && git checkout main && git pull && exit
